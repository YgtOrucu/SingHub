@model CreateAlbumsDto

@{
    ViewData["Title"] = "Yeni Albüm Ekle | SingHub Admin";
    Layout = "~/Views/AdminLayout/Index.cshtml";
    var artists = ViewBag.Artist as List<GetArtistForUpsertDto>;
}

<div class="page-header">
    <div>
        <h1>Yeni Albüm Kaydı</h1>
        <p>Sisteme yeni bir albüm verisi ve sanatçı eşleşmesi ekleyin.</p>
    </div>
    <a href="/Admin/Albums/Index" class="btn-secondary">← Listeye Dön</a>
</div>

<div class="form-container">
    <div class="card form-card">
        <form asp-action="CreateAlbums" method="post" autocomplete="off">

            <div class="form-row">
                <div class="form-group flex-1">
                    <label class="form-label">Albüm Adı <span class="required">*</span></label>
                    <input type="text" asp-for="Title" class="form-control" placeholder="Örn: Midnights, Starboy, Ayla" required />
                </div>

                <div class="form-group flex-1">
                    <label class="form-label">Sanatçı Seçin <span class="required">*</span></label>
                    <select asp-for="ArtistId" class="form-control" required>
                        <option value="">-- Sanatçı Seçiniz --</option>
                        @if (artists != null)
                        {
                            @foreach (var artist in artists)
                            {
                                <option value="@artist.Id">@artist.Name</option>
                            }
                        }
                    </select>
                </div>
            </div>

            <div class="form-row">
                <div class="form-group flex-1">
                    <label class="form-label">Yayın Tarihi <span class="required">*</span></label>
                    <input type="date" asp-for="ReleaseDate" class="form-control" required />
                </div>

                <div class="form-group flex-1">
                    <label class="form-label">Kapak Görsel URL (Cover Image)</label>
                    <input type="text" asp-for="CoverImageUrl" id="albumCoverUrl" class="form-control" placeholder="https://domain.com/cover.jpg" />
                </div>
            </div>

            <div class="album-preview-wrapper">
                <div class="preview-title">Albüm Kapak Önizlemesi</div>
                <div class="album-preview-box">
                    <img id="coverPreview" src="" alt="Kapak Önizleme" style="display:none;" />
                    <span id="coverPlaceholder">Geçerli Görsel URL'si Giriniz</span>
                </div>
            </div>

            <div class="form-actions">
                <a href="/Admin/Albums/Index" class="btn-cancel">İptal</a>
                <button type="submit" class="btn-primary">
                    <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg">
                        <path d="M5 13l4 4L19 7" stroke="#fff" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" />
                    </svg>
                    Albümü Kaydet
                </button>
            </div>
        </form>
    </div>
</div>